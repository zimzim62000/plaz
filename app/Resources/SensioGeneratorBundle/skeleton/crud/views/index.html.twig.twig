{% block extends %}
{{ "{% extends '::base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block title %}" }}
    {{ "{{ '" ~ route_name_prefix ~ ".index.title'|trans }}" }}
{{ "{% endblock title %}" }}

{{ "{% block page %}" }}
    <div class="row">
        <div class="small-10 columns">
            <h1 class="text-center title">{{ entity }} list</h1>
        </div>
    {% if 'new' in actions %}
            <div class="small-2 columns">
                <a class="tiny button" href="{{ "{{ path('" ~ route_name_prefix ~ "_new') }}" }}">
                    {{ "{{ 'button.create'|trans }}" }}
                </a>
            </div>
    {% endif %}
    </div>


    {{ "{{ grid(grid) }}" }}

{{ "{% endblock %}" }}
{% endblock body %}
